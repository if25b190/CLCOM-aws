services:
  bind9:
    image: ubuntu/bind9:latest
    container_name: bind9-server
    environment:
      - BIND9_USER=root
      - TZ=Europe/Vienna
    ports:
      - "53:53/tcp"
      - "53:53/udp"
    volumes:
      - '/srv/bind9/config:/etc/bind'
      - '/srv/bind9/cache:/var/cache/bind'
      - '/srv/bind9/records:/var/lib/bind'
    restart: unless-stopped